# Инциденты

Инциденты автоматически собирают картину того, что произошло в системе: какие метрики отклонились, какие сервисы затронуты, и как это связано. Вместо ручного анализа десятков графиков — одна карточка с ответами на вопросы «что случилось?» и «на что повлияло?».

## Как это работает

Система непрерывно отслеживает отклонения метрик от базовой линии. Когда срабатывает алерт, можно сформировать инцидент — система автоматически найдёт связанные аномалии и соберёт их в карточку. Карточка показывает: какие метрики отклонились, какие сервисы затронуты, и предлагает объяснение причины.

Корреляция аномалий выполняется двумя способами:
- **Статический анализ** — берёт все аномалии сервисов, указанных в алерте
- **ML-алгоритм** — обучается на обратной связи пользователя, чтобы показывать более релевантные аномалии

## Возможности

- Формирование инцидента из алерта или выбранного участка на таймлайне
- Автоматическая корреляция аномалий метрик при формировании инцидента
- Карточка инцидента с визуализацией отклонений, затронутых сервисов и возможных причин
- Два движка корреляции: статический анализ и ML-алгоритм
- Обратная связь для обучения ML-модели — отметьте релевантные и нерелевантные аномалии, модель переобучится и покажет уточнённый результат
- Обогащение инцидента контекстом: релизы, хосты, контейнеры, внешние сервисы
- Объединение и разделение инцидентов

## Планируемые возможности

- Присвоение собственного имени инциденту
- Приём алертов из внешних систем мониторинга (Zabbix, SolarWinds и др.)
- Оценка влияния инцидента на SLO и бизнес-функции
- Создание алерта из инцидента — отправка очищенного уведомления
- Автоматическое создание инцидентов без ручного действия
- Управление жизненным циклом инцидента (статусы, severity)
- Экспорт инцидента во внешнюю ITSM-систему
- Интеграция с CMDB для обогащения контекста и улучшения корреляции

---

## Настройка

### Подключение алертов Grafana

Для работы инцидентов коллектор GMonit должен получать алерты из Grafana. Это требует настройки подключения коллектора к Grafana.

<!-- AGENT:START Опиши как настроить подключение коллектора к Grafana для получения алертов. Найди в коде/конфигурации какие переменные окружения или параметры нужны. -->

Коллектор получает алерты через Grafana Annotations API. Задайте переменные окружения коллектора:

| Переменная | Описание | Пример |
|---|---|---|
| `GRAFANA_API_URL` | URL к Grafana API | `http://grafana:3000/api` |
| `GRAFANA_API_AUTHORIZATION` | Заголовок авторизации | `Bearer glsa_xxxxxxxxxxxx` |
| `ANOMALY_CORRELATION_STATIC_ENGINE_ENABLED` | Включить статический движок корреляции | `true` (по умолчанию) |

Дополнительные параметры детектирования:

| Переменная | Описание | По умолчанию |
|---|---|---|
| `INCIDENT_MANAGER_DETECT_TIME_WINDOW_MINUTE` | Временное окно детектирования (мин) | `30` |
| `INCIDENT_MANAGER_DETECT_TIME_WINDOW_SHIFT` | Сдвиг окна назад от момента алерта (мин) | `15` |

Для получения `GRAFANA_API_AUTHORIZATION`:

1. Откройте Grafana → Administration → Service accounts
2. Создайте сервисный аккаунт с ролью Viewer
3. Сгенерируйте токен
4. Укажите значение в формате `Bearer <токен>`

<!-- AGENT:END -->

### ML-движок корреляции

Для использования ML-алгоритма корреляции необходимо развернуть дополнительный сервис, который выполняет ML-логику.

<!-- AGENT:START Опиши как развернуть ML-сервис для движка корреляции. Найди в коде/документации какой сервис нужен, как он деплоится и какие параметры требует. -->

ML-движок корреляции — это отдельный сервис с HTTP API. Коллектор обращается к нему для анализа аномалий и обработки обратной связи.

**Переменные окружения коллектора:**

| Переменная | Описание | По умолчанию |
|---|---|---|
| `ANOMALY_CORRELATION_ML_ENGINE_ENABLED` | Включить ML-движок | `false` |
| `ANOMALY_CORRELATION_API_URL` | URL ML-сервиса | — |
| `ANOMALY_CORRELATION_API_AUTHORIZATION` | Заголовок авторизации ML-сервиса | — |

**API ML-сервиса:**

Коллектор использует три эндпоинта:
- `GET /healthcheck` — проверка доступности
- `POST /analyze` — корреляция аномалий по датасету
- `POST /analyze-feedback` — переобучение на основе обратной связи

**Параметры статического движка** (работает без ML-сервиса):

| Переменная | Описание | По умолчанию |
|---|---|---|
| `UNIFIED_ENGINE_MAX_INTERVAL_MINUTE` | Макс. интервал между алертами/выбросами для группировки (мин) | `10` |
| `UNIFIED_ENGINE_BFS_DEPTH` | Глубина обхода графа связности сервисов | `0` |
| `UNIFIED_ENGINE_TOP_OUTLIERS` | Кол-во топ-выбросов как начальных точек группировки | `20` |

> Статический движок (`[GMonit] Unified anomaly grouping`) включён по умолчанию и не требует дополнительных сервисов. ML-движок (`[GMonit] Anomaly correlation`) включается дополнительно.

<!-- AGENT:END -->

---

## Просмотреть таймлайн алертов

Таймлайн показывает хронологию алертов системы — точки входа для формирования инцидентов.

<!-- AGENT:START Опиши пошагово как открыть таймлайн алертов в интерфейсе. Покажи где он находится в навигации и что пользователь видит на экране. Приложи скриншоты. -->

1. В боковом меню GMonit нажмите **Инциденты**
2. Откроется страница с тремя панелями:
   - **Алерты** — таймлайн алертов Grafana. По строкам — пары «сервис + метрика» (например, `api-services APMApdex`). Красные полоски — периоды, когда алерт находился в состоянии `alerting`
   - **Выбросы** — таймлайн аномальных отклонений метрик. По строкам — имена сервисов. Цветные полоски — обнаруженные выбросы
   - **Список инцидентов** — таблица сформированных инцидентов с колонками: Имя, Начало, Конец, Длительность, Кол-во алертов, Приложения

![Страница инцидентов с таймлайном алертов и выбросов](./screenshots/incidents_timeline.png)

3. Вверху страницы:
   - **Движок** — выбор движка корреляции (выпадающий список)
   - **Временной диапазон** — период отображения данных (по умолчанию «Последние 24 часа»)
   - Кнопки навигации по времени (стрелки влево/вправо), лупа для зума

<!-- AGENT:END -->

## Сформировать инцидент из алертов

Из одного или нескольких алертов можно запустить корреляцию аномалий и сформировать инцидент. Если в рамках небольшого временного окна было несколько алертов, они попадут в один инцидент.

<!-- AGENT:START Опиши пошагово как выбрать алерты и создать инцидент. Покажи какие опции доступны при создании (выбор движка корреляции и т.д.). Приложи скриншоты. -->

1. На странице **Инциденты** выберите **движок корреляции** в выпадающем списке «Движок» вверху страницы:
   - **[GMonit] Unified anomaly grouping** — статический алгоритм (по умолчанию)
   - **[GMonit] Anomaly correlation** — ML-алгоритм (если включён)

![Выбор движка корреляции](./screenshots/incidents_engine_selector.png)

2. Нажмите на красную полоску алерта в панели **Алерты**. Появится всплывающее окно с информацией:
   - Временной диапазон алерта
   - Название метрики и состояние (`alerting`)
   - Длительность алерта
   - Ссылка **«Проанализировать период»**

![Контекстное меню алерта](./screenshots/incidents_alert_popup.png)

3. Нажмите **«Проанализировать период»** — система запустит корреляцию аномалий за выбранный период с помощью указанного движка
4. После обработки инцидент появится в таблице **Список инцидентов** внизу страницы

> Если в выбранном временном окне несколько алертов, они автоматически группируются в один инцидент.

<!-- AGENT:END -->

## Просмотреть карточку инцидента

Карточка собирает все данные об инциденте: отклонения метрик, затронутые сервисы, возможные причины.

<!-- AGENT:START Опиши элементы карточки инцидента: что показывает каждая секция, как читать графики отклонений, какие данные обогащают инцидент (релизы, хосты, контейнеры, внешние сервисы). Приложи скриншоты. -->

Нажмите на строку инцидента в таблице **Список инцидентов** — откроется карточка.

![Карточка инцидента](./screenshots/incidents_card.png)

Карточка состоит из следующих секций (сверху вниз):

**Статистика** (верхняя полоса):
- **Алерты** — количество алертов в инциденте
- **Сервисы** — количество затронутых приложений
- **Продолжительность** — общая длительность инцидента
- **Начало инцидента** / **Конец инцидента** — временные границы

**Пиковые метрики** (три панели):
- **Пиковый RPM** — максимальное и минимальное число запросов в минуту по затронутым сервисам с процентом отклонения от нормы
- **Пиковое время обработки запросов** — макс. и мин. время отклика с процентом отклонения
- **Пики ошибок в транзакциях** — пиковая частота ошибок (или «Нет ошибок»)

**Таймлайн инцидента** — хронологический список событий:
- Обнаружение инцидента
- Начало и завершение каждого алерта (с указанием метрики)
- Конец инцидента

**Радиус поражения** — визуализация затронутых сервисов в виде кольцевой диаграммы. Показывает RPM и среднее время отклика каждого сервиса. Размер и цвет сегментов отражают степень влияния.

**Дополнительные секции:**
- **Группы трассировок** — связанные группы трейсов за период инцидента
- **Транзакции** — затронутые транзакции
- **Частота ошибок транзакций по приложениям** — графики ошибок
- **Отчёты об ошибках** — детальные отчёты об ошибках

<!-- AGENT:END -->

## Дать обратную связь на инцидент

Если корреляция включила нерелевантные данные или пропустила важные — можно обучить ML-модель через обратную связь.

<!-- AGENT:START Опиши пошагово как оставить обратную связь: где кнопка, как отметить релевантные/нерелевантные аномалии и сервисы, как отправить и что произойдёт после (модель переобучится и покажет обновлённый результат). Приложи скриншоты. -->

> Обратная связь доступна только для инцидентов, созданных ML-движком (`[GMonit] Anomaly correlation`).

1. Откройте карточку инцидента
2. Нажмите кнопку **«Оценка»** в правом верхнем углу карточки

![Режим обратной связи](./screenshots/incidents_feedback.png)

3. Интерфейс переключится в режим обратной связи:
   - В таймлайне инцидента появится колонка **«Фидбек»** с тремя кнопками для каждого события:
     - ✓ (галочка) — событие **релевантно** инциденту
     - ⊘ (перечёркнутый круг) — событие **нерелевантно**, не относится к инциденту
     - ✎ (стрелка) — **переместить** событие в другой инцидент
   - Вверху появятся кнопки действий:
     - **«Слияние»** — объединить текущий инцидент с другим
     - **«Отправить»** — отправить обратную связь
     - **«Отменить»** — выйти без сохранения

4. Отметьте релевантность каждого события, нажимая соответствующие кнопки
5. Нажмите **«Отправить»**

После отправки ML-модель переобучится с учётом вашей обратной связи. Результат корреляции обновится — состав аномалий в инциденте может измениться.

<!-- AGENT:END -->

## Присвоить имя инциденту *(в разработке)*

Возможность дать инциденту понятное название для удобства поиска и коммуникации.

<!-- AGENT:START Опиши как присвоить имя инциденту. Приложи скриншоты. -->
*Функция в разработке. Раздел будет дополнен после релиза.*
<!-- AGENT:END -->

## Приём алертов из внешних систем *(в разработке)*

Получение алертов из внешних систем мониторинга (Zabbix, SolarWinds и др.) через webhook для включения в корреляцию инцидентов.

<!-- AGENT:START Опиши как настроить приём внешних алертов: какие форматы поддерживаются, как настроить webhook, как алерты попадают в таймлайн. -->
*Функция в разработке. Раздел будет дополнен после релиза.*
<!-- AGENT:END -->

## Оценка влияния на SLO *(в разработке)*

Связь инцидента с затронутыми SLO и бизнес-функциями для оценки масштаба влияния.

<!-- AGENT:START Опиши как в карточке инцидента отображается влияние на SLO и бизнес-функции. -->
*Функция в разработке. Раздел будет дополнен после релиза.*
<!-- AGENT:END -->

## Создать алерт из инцидента *(в разработке)*

Отправка очищенного уведомления на основе сформированного инцидента.

<!-- AGENT:START Опиши как создать алерт из инцидента: где кнопка, какие параметры, куда отправляется. -->
*Функция в разработке. Раздел будет дополнен после релиза.*
<!-- AGENT:END -->

## Автоматическое создание инцидентов *(в разработке)*

Инциденты формируются автоматически при срабатывании алертов, без ручного действия.

<!-- AGENT:START Опиши как настроить автоматическое создание инцидентов: какие правила, какие параметры. -->
*Функция в разработке. Раздел будет дополнен после релиза.*
<!-- AGENT:END -->

## Управление жизненным циклом *(в разработке)*

Статусная модель инцидента (open, mitigating, resolved, closed) и уровни severity (SEV1–SEV5).

<!-- AGENT:START Опиши как управлять статусом и severity инцидента. -->
*Функция в разработке. Раздел будет дополнен после релиза.*
<!-- AGENT:END -->

## Экспорт в ITSM *(в разработке)*

Отправка инцидента во внешнюю ITSM-систему.

<!-- AGENT:START Опиши как экспортировать инцидент в ITSM: какие системы поддерживаются, как настроить интеграцию. -->
*Функция в разработке. Раздел будет дополнен после релиза.*
<!-- AGENT:END -->

## Интеграция с CMDB *(в разработке)*

Подключение CMDB для обогащения контекста инцидента и улучшения корреляции.

<!-- AGENT:START Опиши как подключить CMDB и как данные из неё используются в инцидентах. -->
*Функция в разработке. Раздел будет дополнен после релиза.*
<!-- AGENT:END -->
