# Бизнес-мониторинг — Использование

## Собрать бизнес-процесс

Описать последовательность шагов, по которой платформа будет отслеживать экземпляры: заказ, регистрацию, платёж.

<!-- STEP_GUIDE:START Пошаговая сборка бизнес-процесса в конструкторе: выбор бизнес-системы, чтение карты переходов между реальными этапами, добавление шага с карты и вручную, изменение порядка шагов, условия шага и подсказки по доступным значениям, переключение между формой и текстовым представлением, сохранение. Скриншоты. -->
<!-- STEP_GUIDE:END -->

## Оценить состояние бизнес-процессов

Понять за один взгляд, сколько экземпляров каждого процесса прошло полностью, сколько застряло и как это соотносится с предыдущим периодом.

<!-- STEP_GUIDE:START Обзор состояния бизнес-процессов: состав списка процессов бизнес-системы, сводные показатели по каждому, сводка в шапке процесса, сравнение с предыдущим периодом, влияние выбранного временного диапазона. Скриншоты. -->
<!-- STEP_GUIDE:END -->

## Найти шаг с наибольшими потерями

Определить, на каком этапе процесс теряет больше всего экземпляров, и связать это с состоянием сервисов.

<!-- STEP_GUIDE:START Работа с воронкой бизнес-процесса: чтение воронки и полосы шагов, интерпретация перепада между соседними шагами, влияние разных источников событий на величину перепада, состояние задействованных сервисов, переход к списку экземпляров. Скриншоты. -->
<!-- STEP_GUIDE:END -->

## Разобрать причины незавершённых сессий

Понять, почему экземпляры не дошли до конца: отказ с известной причиной, подозрительное событие или простое зависание.

<!-- STEP_GUIDE:START Разбор незавершённых сессий: источники причин и их приоритет, распределение причин, режимы просмотра, таблица экземпляров с причиной, состояние страницы при ненастроенных правилах, переход к экземпляру. Скриншоты. -->
<!-- STEP_GUIDE:END -->

## Найти зависшие экземпляры

Получить список экземпляров, которые стоят на шаге дольше допустимого — тех, с которыми нужно что-то делать прямо сейчас.

<!-- STEP_GUIDE:START Операционный контроль: распределение экземпляров по текущим шагам, распределение зависаний по шагам, список зависших экземпляров и порядок его сортировки, время без продвижения, переход к экземпляру. Скриншоты. -->
<!-- STEP_GUIDE:END -->

## Проанализировать время прохождения

Понять, сколько времени занимает процесс целиком и какой шаг вносит основной вклад в задержку.

<!-- STEP_GUIDE:START Анализ времени прохождения: тренды среднего и медианного времени, тренд по отдельным шагам, тепловая карта шагов и правила её раскраски, распределение длительностей, самые долгие экземпляры, ограничение выборки завершёнными экземплярами. Скриншоты. -->
<!-- STEP_GUIDE:END -->

## Разобрать отдельный экземпляр

Посмотреть хронологию конкретного заказа или сессии и провалиться до технических деталей — трассировки и браузерной сессии.

<!-- STEP_GUIDE:START Разбор отдельного экземпляра: точки входа из таблиц экземпляров, поиск по идентификатору, состав сводки, полоса пройденных шагов, хронология прохождения с длительностями, переходы к распределённой трассировке и к браузерной сессии. Скриншоты. -->
<!-- STEP_GUIDE:END -->

## Собрать собственный дашборд

Перенести панели бизнес-мониторинга в обычный дашборд Grafana — рядом с другими данными клиента.

<!-- STEP_GUIDE:START Сборка собственного дашборда на данных бизнес-мониторинга: что переносится копированием панели и что нет, порядок копирования, построение панели с нуля, диагностика ошибок и пустого результата, известные ограничения. -->

Четыре вкладки бизнес-процесса — часть продукта, они одинаковы у всех клиентов; меняется только описание бизнес-системы и процесса. Всё, что нужно **сверх** этих вкладок, собирается как обычный дашборд Grafana. TypeScript писать не нужно, SQL — по желанию.

Предварительно на дашборде должны быть объявлены переменные `bs` и `bp` — см. [Переменные дашборда](/modules/business-monitoring/setup.md#Переменные-дашборда).

### Копирование панели

Основной способ. Собирать запрос с нуля тоже можно, но копия почти всегда быстрее и надёжнее.

1. Откройте нужную вкладку бизнес-процесса.
2. В меню панели — **«Копировать панель»**.
3. В своём дашборде — обычная вставка Grafana (Paste panel).

Больше ничего править не нужно: в теле запроса уже стоят ссылки на переменные `bs` и `bp`, то есть панель сразу подчиняется выбору на дашборде.

**Что переезжает вместе с панелью:** запрос, ссылка на процесс через переменные, тип панели, настройки полей и трансформации. Параметры, которые коллектор вычисляет сам — источники событий, пороги — намеренно не переносятся: иначе панель осталась бы привязана к тому процессу, который был открыт в момент копирования.

**Что не переезжает:** всё, что в интерфейсе является кодом, а не настройкой. Это переходы из панели в боковую панель с трассировками — на дашборде их некуда открывать; панель нарисуется, но без кликов. Сюда же относится поле поиска в заголовке таблицы «Экземпляры».

**Поиск по идентификатору в скопированной таблице.** Сам фильтр — обычная трансформация Grafana, поэтому его можно сделать своими руками, один раз на дашборд:

1. Dashboard settings → Variables → New → тип **Textbox**, имя `instance_search`. Grafana покажет поле ввода в шапке дашборда.
2. На панели: Transform → **Filter data by values** → Include, поле `instance_id`, match **substring**, значение `${instance_search}`.

Пустое значение в `substring` означает «подходит всё», поэтому пока в поле ничего не введено, таблица показывает все строки.

### Панель с нуля

Датасорс **Infinity**, метод `POST`:

- URL: `/business-monitoring/business-process/query`
- Type **JSON**, Parser **backend**, **Root selector: `data`**
- Body content type: `application/json`
- Body:

```json
{
  "sql_template": "...",
  "bs_id": "${bs}",
  "bp_id": "${bp}",
  "account_id": "${__org}",
  "from": "${__from}",
  "to": "${__to}",
  "interval_ms": "${__interval_ms}"
}
```

`sql_template` проще всего взять из скопированной панели. Он написан на макро-языке: макросы разворачиваются на стороне коллектора в запросы по источникам событий и условиям шагов, поэтому SQL без них бизнес-события не увидит.

> Базовый URL датасорса Infinity уже включает префикс `/grafana`. Если обращаетесь к коллектору напрямую, путь будет `/grafana/business-monitoring/business-process/query`.

### Если панель показывает «Bad request»

Это не пустой результат, а отказ коллектора, и почти всегда — идентификатор, который не подставился. Посмотрите **Panel → Query inspector → JSON**, поле `bs_id`:

| Что в `bs_id` | Что это значит |
|---|---|
| `${bs}` | переменной `bs` на дашборде **нет**. `var-bs=…` в адресе ничего не создаёт — переменную нужно объявить |
| `""` | переменная есть, но пустая — у Query-переменной не выбрано значение |
| настоящий идентификатор | дело не в идентификаторе, см. следующий раздел |

Текст ошибки коллектор возвращает всегда, но Infinity его не показывает — отсюда безликое «Bad request».

### Если панель пустая

Infinity показывает только код ответа, без текста ошибки коллектора. Поэтому есть отдельная ручка, которая разворачивает шаблон и **не выполняет** его:

- URL: `/business-monitoring/business-process/expand`
- Method POST, тело — то же самое
- Type **JSON**, **Root selector: пусто** (у ответа нет ключа `data`; `data` здесь даст ошибку JSONata «no results found»)

Вернётся одна строка с колонкой `formatted-sql` — готовый SQL. Дальше его можно выполнить руками в ClickHouse.

Частые причины пустого результата:

- окно времени, в котором у процесса нет экземпляров — это ответ, а не поломка;
- неверный идентификатор системы или процесса: коллектор вернёт ошибку и перечислит существующие;
- имя приложения в описании бизнес-процесса не совпадает с реальным — тогда процесс «схлопывается» в нули без всякой ошибки.

### Известные шероховатости

- **Длительности в стоковых панелях по-английски.** Единица `s` в Grafana после минуты пишет «2.28 hours» независимо от языка интерфейса. Типы панелей GMONIT форматируют длительности сами.
- **Тепловая карта: относительные пороги не переносятся.** Если у шага в конфигурации заданы пороги длительности, цвета уедут вместе с панелью. Если нет — цвет считается от собственного диапазона шага на экране, такое в дашборд не перенести; перенастройте пороги в field config.
- **«Сравнение с предыдущим периодом» не копируется осмысленно.** Панель делает два запроса по двум разным окнам, а сдвинутое окно макросами Grafana не выражается.
- **Столбцы приходят по алфавиту.** Infinity отдаёт поля в алфавитном порядке, а не в порядке SELECT. Если для панели важен порядок — поправьте трансформацией «Organize fields».

<!-- STEP_GUIDE:END -->
