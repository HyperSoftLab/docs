# MCP — Настройка

## Подключение

Включение MCP-эндпоинта на коллекторе.

<!-- STEP_GUIDE:START Включение MCP на коллекторе: переменные окружения, минимальная версия коллектора, минимальные версии для отдельных возможностей, проверка доступности эндпоинта. -->

Нужен коллектор GMONIT версии **>= v4-6943**. Отдельные возможности появились позже:

| Возможность | Минимальная версия |
|---|---|
| MCP-сервер, инструменты по приложениям и данным | `v4-6943` |
| [Встроенная экспертиза](#встроенная-экспертиза) и [инструменты Grafana](#инструменты-grafana) | `v4-7248` |
| [Аутентификация по токену Grafana](#аутентификация-по-токену-grafana) и [роли](#разграничение-прав-роли) | `v4-7455` |
| [Аудит событий](#аудит-событий) | `v4-7458` |

Переменные окружения коллектора:

| Переменная | Описание | Пример |
|---|---|---|
| `MCP_ENABLED` | Включить MCP-модуль | `true` |
| `MCP_AUTH_TOKEN` | Bearer-токен для аутентификации MCP-клиентов. Любая непустая строка | `mcp-7Hq3...` |
| `CLICKHOUSE_READ_USER` | Пользователь ClickHouse, от имени которого MCP выполняет запросы | `gmonit_readonly` |
| `CLICKHOUSE_READ_KEY` | Пароль этого пользователя | `<секрет>` |

> При `MCP_ENABLED=true` `CLICKHOUSE_READ_USER` и `CLICKHOUSE_READ_KEY` становятся обязательными — без них коллектор не стартует. Кого выбирать в качестве этого пользователя — см. [Read-only пользователь ClickHouse](#read-only-пользователь-clickhouse).

После рестарта коллектора эндпоинт доступен по адресу `https://<адрес-коллектора>/mcp` (Streamable HTTP).

Быстрая проверка доступности:

```bash
curl -i -H "Authorization: Bearer <MCP_AUTH_TOKEN>" \
  https://<адрес-коллектора>/mcp
```

Коды ответа: `200`/`405` — эндпоинт жив (MCP принимает `POST`), `401` — неверный токен.

<!-- STEP_GUIDE:END -->

## Встроенная экспертиза

Вместе с инструментами MCP отдаёт агенту доменные знания о GMONIT — методики расследования, карту таблиц, язык запросов. Отдельной установки не требуется.

<!-- STEP_GUIDE:START Поставка доменных знаний вместе с MCP: отсутствие отдельной установки и настройки, поставка в составе образа коллектора, механизм доставки агенту, отсутствие требований к правам и сетевому доступу, поведение при отключении. -->

Настраивать нечего — знания едут в образе коллектора и включаются вместе с MCP.

Как это устроено:

- При подключении клиента коллектор отдаёт агенту краткий свод правил работы с GMONIT, а полные материалы (методики расследования, карта данных по всем источникам, язык запросов, шаблоны) публикует как ресурсы MCP
- Агент подтягивает нужный материал сам, по мере необходимости — целиком всё не грузится
- Отдельного канала наружу нет: материалы отдаются по тому же соединению и тому же токену, что и инструменты. Дополнительных прав, сетевых доступов и внешних сервисов не требуется

Что это даёт пользователю и как выглядит в работе — см. [Использование](/modules/mcp/guide.md#встроенная-экспертиза).

Отключить отдельно нельзя — экспертиза неотделима от MCP. Если в образе коллектора материалов нет (сборка старше `v4-7248`), MCP работает как обычный набор инструментов, без ресурсов.

<!-- STEP_GUIDE:END -->

## Доступность инструментов

Часть инструментов относится к отдельным модулям коллектора и появляется только вместе с ними.

<!-- STEP_GUIDE:START Зависимость состава инструментов MCP от включённых модулей коллектора: соответствие групп инструментов переменным окружения, поведение при выключенном модуле, инструменты без зависимостей. -->

Отдельно для MCP ничего включать не нужно: инструмент появляется, если включён модуль, к которому он относится.

| Инструменты | Переменная окружения | По умолчанию |
|---|---|---|
| Трассировки | `DISTRIBUTED_TRACING_V2_ENABLED` | `true` |
| Логи приложений | `LOGS_ENABLED` | `true` |
| Браузерный мониторинг | `BROWSER_MONITORING_ENABLED` | `true` |
| Бизнес-мониторинг | `BUSINESS_MONITORING_ENABLED` | `true` |
| Профилирование | `PROFILING_ENABLED` | `true` |
| Инциденты | `INCIDENT_MANAGER_ENABLED` | `true` |
| Запрос разбора первопричины инцидента | `INCIDENT_LLM_RCA_ENABLED` и настроенная модель | `false` |

Задавать переменные обычно не нужно: все модули, кроме разбора первопричины, включены по умолчанию — в таблице они на случай, если модуль был выключен намеренно.

**На лицензии lite все модули из таблицы выключены независимо от переменных окружения.** Переменная включает модуль только при полной лицензии.

Работают всегда, без отдельного включения: состояние приложений, транзакции, ошибки, обращения к БД, карта сервисов, инфраструктура, серверы баз данных, мобильный мониторинг, схема данных и проверка здоровья платформы.

Выключенный модуль не убирает инструмент из списка: агент по-прежнему видит его и при попытке вызова получает сообщение с именем переменной, которую нужно задать. Это осознанно — исчезнувший инструмент выглядит для модели как отсутствующая возможность, и она начинает искать обходные пути вместо того, чтобы сообщить вам о настройке.

Разбор первопричины требует не только флага, но и параметров доступа к языковой модели — иначе коллектор не стартует. Как включить — см. [Инциденты → Разбор первопричины через LLM](/modules/incidents/setup.md#разбор-первопричины-через-llm).

<!-- STEP_GUIDE:END -->

## Готовые сценарии

Вместе с инструментами и знаниями подключение отдаёт наработанные методики расследования — клиент показывает их списком команд.

<!-- STEP_GUIDE:START Поставка готовых сценариев вместе с MCP: отсутствие отдельной установки, состав, как выглядят в клиенте, требования к правам, поведение при отсутствии в сборке. -->

Настраивать нечего — сценарии едут в образе коллектора и включаются вместе с MCP.

Поставляются три: поиск первопричины, обзор текущих проблем и отчёт о здоровье за период. Клиент показывает их как команды (в Claude Code — через `/`), с подсказками по аргументам; аргументы необязательны.

Дополнительных прав и сетевых доступов не требуют: отдаются по тому же соединению и тому же токену, что и инструменты. Сами по себе данные не читают — работу выполняют те же инструменты, с теми же ограничениями по ролям.

Что делает каждый сценарий — см. [Использование](/modules/mcp/guide.md#готовые-сценарии).

Если в сборке коллектора сценариев нет, клиент просто не покажет список команд; инструменты и знания при этом работают.

<!-- STEP_GUIDE:END -->

## Инструменты Grafana

Помимо запросов к данным MCP умеет проксировать Grafana: активные оповещения, правила оповещений, дашборды, аннотации. Группа включается отдельно.

<!-- STEP_GUIDE:START Включение группы инструментов, работающих через Grafana: переменные окружения и условие включения группы, состав группы, поведение при выключенной группе, взаимосвязь с аутентификацией по токену Grafana и с ролями. -->

| Переменная | Описание | Пример |
|---|---|---|
| `GRAFANA_API_URL` | Базовый URL Grafana **включая суффикс `/api`** | `https://gmonit.example.com/api` |
| `GRAFANA_API_AUTHORIZATION` | Заголовок авторизации сервис-аккаунта Grafana целиком, вместе со схемой | `Bearer glsa_...` |

Группа включается, только если заданы **обе** переменные. Если хотя бы одной нет — инструменты остаются в наборе, но на любой вызов агент получает сообщение о том, что они выключены, и передаёт его вам.

Что появляется при включении:

| Группа | Инструменты |
|---|---|
| Оповещения | активные оповещения; просмотр, создание, изменение, удаление правил |
| Дашборды | поиск, чтение полной модели, создание и изменение |
| Аннотации | поиск отметок за период, создание отметки |

Сервис-аккаунт из `GRAFANA_API_AUTHORIZATION` должен иметь права на те операции, которые вы разрешаете агенту. Для полного набора нужна роль **Admin**: правила оповещений заводятся через provisioning-API Grafana, а он администраторский. Если правила через агента не нужны — хватит **Editor**.

**Связь с аутентификацией.** `GRAFANA_API_URL` используется сразу двумя механизмами: этой группой инструментов и [проверкой персональных токенов](#аутентификация-по-токену-grafana). Задать URL без `GRAFANA_API_AUTHORIZATION` — валидный сценарий: персональные токены будут проверяться, а инструменты Grafana останутся выключенными.

**Под чьим токеном идут запросы.** Если клиент вошёл по [персональному токену Grafana](#аутентификация-по-токену-grafana), операции проксируются в Grafana **под его токеном** — Grafana дополнительно проверяет права этого пользователя. Если клиент вошёл по общему `MCP_AUTH_TOKEN`, используется сервис-аккаунт из `GRAFANA_API_AUTHORIZATION`.

<!-- STEP_GUIDE:END -->

## Аутентификация по токену Grafana

Помимо общего токена MCP умеет пускать клиентов по персональному токену сервис-аккаунта Grafana — тогда каждый пользователь ходит под своей учёткой, а не под общим секретом. Оба способа работают одновременно.

<!-- STEP_GUIDE:START Аутентификация MCP-клиентов персональным токеном сервис-аккаунта Grafana как альтернатива общему токену: два принимаемых способа по логике ИЛИ, проверка токена обращением к Grafana, кеширование результата проверки и его время жизни, распространение смены прав и отзыва токена во времени, необходимые параметры подключения к Grafana. -->

MCP принимает Bearer-токен одним из двух способов (логика ИЛИ):

| Способ | Что за токен | Когда использовать |
|---|---|---|
| Общий токен | значение `MCP_AUTH_TOKEN` | сервисный/административный доступ, один секрет на всех |
| Персональный токен Grafana | токен сервис-аккаунта Grafana (`glsa_...`) | доступ под конкретным пользователем и с его правами |

Достаточно настроить **хотя бы один** способ. Если не задан ни `MCP_AUTH_TOKEN`, ни `GRAFANA_API_URL` — коллектор не стартует.

Переменные окружения для проверки токенов Grafana:

| Переменная | Описание | По умолчанию |
|---|---|---|
| `GRAFANA_API_URL` | Базовый URL Grafana (включая суффикс `/api`), к которой MCP обращается для проверки токена | — |
| `MCP_GRAFANA_AUTH_CACHE_TTL_MS` | Сколько миллисекунд кэшировать результат проверки токена | `60000` |

**Как это работает.** На входящий запрос MCP сперва сравнивает токен с общим `MCP_AUTH_TOKEN`; если не совпал — проверяет его как токен Grafana, обращаясь к Grafana API. Результат проверки (и успешный, и неуспешный) кэшируется на `MCP_GRAFANA_AUTH_CACHE_TTL_MS`, чтобы не обращаться к Grafana на каждый запрос.

**Как выпустить персональный токен.** В Grafana создайте сервис-аккаунт с нужной ролью (Viewer / Editor / Admin — влияет на доступные инструменты, см. [Разграничение прав](#разграничение-прав-роли)) и выпустите для него токен вида `glsa_...` в разделе сервис-аккаунтов администрирования Grafana. Этот токен клиент передаёт в заголовке `Authorization: Bearer glsa_...`.

**Смена прав и отзыв.** Изменение роли сервис-аккаунта или удаление токена в Grafana вступают в силу в пределах `MCP_GRAFANA_AUTH_CACHE_TTL_MS` (по умолчанию до минуты) — пока не истечёт кэш. После этого отозванный токен получает `401`.

<!-- STEP_GUIDE:END -->

## Разграничение прав (роли)

При аутентификации по токену Grafana набор доступных инструментов зависит от роли вызывающего в Grafana — низкопривилегированный пользователь не получит доступ к чувствительным операциям.

<!-- STEP_GUIDE:START Разграничение доступа к инструментам MCP по роли вызывающего (просмотр, редактирование, администрирование): какие группы инструментов доступны каждой роли, откуда берётся роль, ограничение произвольного SQL уровнем администратора, проксирование операций в Grafana под токеном самого пользователя и дополнительная проверка прав на стороне Grafana. -->

При аутентификации по токену Grafana MCP определяет роль вызывающего по его правам в Grafana и по ней ограничивает доступные инструменты. Уровни включают друг друга:

| Роль | Доступные инструменты |
|---|---|
| **Viewer** | Всё чтение: состояние приложений, транзакции, ошибки, трассировки, инфраструктура, серверы БД, логи, браузер, мобильные, инциденты, цели, бизнес-процессы, настройки, схема данных; из Grafana — аннотации, группы алертов, поиск и просмотр дашбордов |
| **Editor** | Всё из Viewer + изменения: правила алертов, дашборды, аннотации, пороги и подтверждение оповещений, курирование инцидентов и запрос разбора, создание и пауза целей, определения бизнес-процессов, метки и конфигурация агентов, запуск профилирования |
| **Admin** | Всё из Editor + удаление инцидентов и целей, изменение сроков хранения, произвольный SQL (`execute_query`, `bm_query`) |

Правило простое: инструменты с суффиксом `_read` — Viewer, `_manage` — Editor, `_admin` — Admin.

- Роль определяется правами сервис-аккаунта в Grafana.
- Общий токен `MCP_AUTH_TOKEN` даёт полный доступ (эквивалент Admin).
- Изменяющие операции проксируются в Grafana **под токеном самого пользователя** — поэтому Grafana дополнительно проверяет его права (двойной контроль).
- Если роли не хватает прав на инструмент, агент получает сообщение вида `Forbidden: tool '<имя>' requires role '<роль>'` и сообщает вам.

<!-- STEP_GUIDE:END -->

## Read-only пользователь ClickHouse

MCP выполняет все запросы от имени пользователя ClickHouse, указанного в [подключении](#подключение). Чтобы AI-агент не мог изменить или удалить данные, выделите ему отдельную учётку с правом только на чтение.

<!-- STEP_GUIDE:START Использование выделенного read-only пользователя ClickHouse для MCP: мотивация, требуемые права на стороне ClickHouse, взаимодействие с режимом произвольного SQL. -->

**Зачем.** Все запросы MCP идут от одного пользователя ClickHouse, заданного в `CLICKHOUSE_READ_USER`. Если это учётка с правами на запись, ошибка агента или неудачно сформулированный запрос могут изменить или удалить данные. Особенно важно при включённом [режиме произвольного SQL](#режим-произвольного-sql) — read-only пользователь там единственная реальная защита от разрушительных запросов.

**Какие права нужны.** Пользователю достаточно:

- `SELECT` на базу с данными GMONIT
- `SELECT` на системные таблицы ClickHouse — без них не работает диагностика [здоровья системы](/modules/mcp/guide.md#здоровье-системы) и [список таблиц](/modules/mcp/guide.md#схема-данных)
- `SHOW TABLES` / `SHOW COLUMNS` — для чтения структуры таблиц

Права на `INSERT`, `ALTER`, `DROP`, `TRUNCATE` и создание объектов давать не нужно.

Дополнительно стоит ограничить пользователя квотами и настройками профиля ClickHouse (максимальное время выполнения, объём читаемых строк) — это защищает кластер от тяжёлого запроса, который агент напишет по неосторожности.

**Связка с произвольным SQL.** Read-only пользователь и `MCP_RAW_SQL_ENABLED` — независимые настройки. Read-only ограничивает, *что* можно сделать; флаг SQL — *чем* можно это делать. Безопасная комбинация для production: read-only пользователь есть, произвольный SQL выключен. Если SQL включается — read-only обязателен.

<!-- STEP_GUIDE:END -->

## Режим произвольного SQL

По умолчанию агент работает только через структурированные инструменты (Apdex приложений, схема таблиц, состояние pipeline). Произвольный SQL включается отдельно — это компромисс между гибкостью и безопасностью.

<!-- STEP_GUIDE:START Включение режима произвольного SQL: переменная окружения, ограничения по объёму результата, риски, рекомендуемая связка с read-only пользователем, ограничение инструмента ролью администратора при аутентификации по токену Grafana. -->

| Переменная | Описание | По умолчанию |
|---|---|---|
| `MCP_RAW_SQL_ENABLED` | Разрешить инструмент `execute_query` с произвольным SQL | `false` |

Если флаг не выставлен, инструмент в наборе MCP остаётся, но любой его вызов агенту возвращает сообщение о том, что произвольный SQL отключён — у агента не получится обойти ограничение, переформулировав запрос.

При включении:

- Результат любого запроса режется на 200 строках
- Агент может выполнить всё, что позволят права пользователя ClickHouse — поэтому включайте только в связке с [read-only пользователем](#read-only-пользователь-clickhouse)
- Полезно для ad-hoc исследования данных, для production-окружений лучше держать выключенным
- При аутентификации по [токену Grafana](#аутентификация-по-токену-grafana) инструмент доступен только роли **Admin** (см. [Разграничение прав](#разграничение-прав-роли))

<!-- STEP_GUIDE:END -->

## Аудит событий

MCP умеет вести структурированный журнал событий (аутентификация, вызовы инструментов, выгрузки данных, ошибки) для передачи в SIEM. По умолчанию журнал выключен.

<!-- STEP_GUIDE:START Журналирование событий MCP для SIEM: включение аудита, вывод в выделенный поток или отдельный файл, состав события и его обязательные поля, отделение потока аудита от эксплуатационного вывода приложения, ответственность среды исполнения за доставку, ротацию и хранение журнала. -->

| Переменная | Описание | По умолчанию |
|---|---|---|
| `MCP_AUDIT_ENABLED` | Включить журналирование событий MCP | `false` |
| `MCP_AUDIT_FILE` | Путь к файлу журнала аудита. Если не задан — журнал пишется в stdout | — |

Что попадает в журнал: аутентификация (успех/отказ), вызовы инструментов, выгрузки данных (с текстом SQL-запроса), отказы авторизации по роли, ошибки обработки.

Каждое событие — одна строка JSON (UTF-8) с обязательными полями: `timestamp` (с точностью до мс и часовым поясом), `event_type`, `source_system`, `severity`. Формат совместим с KUMA.

**Выделенный поток.** Задавайте `MCP_AUDIT_FILE` — тогда журнал аудита пишется в отдельный файл, изолированный от эксплуатационного и диагностического вывода приложения. Это позволяет SIEM контролировать сам факт поступления событий (алерт на прекращение аудита) и не смешивать аудит с прочими логами.

**Доставка, ротация и хранение — на стороне среды исполнения.** Приложение только дозаписывает события в файл (append) и не управляет ротацией. Передачу в SIEM, ротацию и срок хранения настраивайте средствами окружения — например, `logrotate` в режиме `copytruncate` плюс syslog-агент (rsyslog и подобные). Append-режим корректно переживает `copytruncate`.

<!-- STEP_GUIDE:END -->

## Подключение клиента

Подключение AI-агента к развёрнутому MCP-эндпоинту.

<!-- STEP_GUIDE:START Подключение AI-агента к MCP-эндпоинту: пример команды для Claude Code, проверка подключения, подключение нескольких стендов одновременно, удаление. -->

Для Claude Code (CLI):

```bash
claude mcp add gmonit https://<адрес-коллектора>/mcp --header "Authorization: Bearer <MCP_AUTH_TOKEN>" --transport http  
```

`<MCP_AUTH_TOKEN>` и адрес коллектора возьмите у администратора стенда.

Проверить подключение:

```bash
claude mcp list
```

Несколько стендов можно держать одновременно — каждому своё имя:

```bash
claude mcp add gmonit-dev https://<dev-collector>/mcp --header "Authorization: Bearer <TOKEN_DEV>" --transport http
claude mcp add gmonit-prod https://<prod-collector>/mcp --header "Authorization: Bearer <TOKEN_PROD>" --transport http  
```

Удалить:

```bash
claude mcp remove gmonit
```

Другие MCP-клиенты (Codex, IDE-плагины) подключаются так же — указывается транспорт `HTTP`, URL вида `https://<адрес-коллектора>/mcp` и заголовок `Authorization: Bearer <MCP_AUTH_TOKEN>`.

<!-- STEP_GUIDE:END -->
